name: Build and deploy

on:
  push:
    branches: [main]

env:
  SITE_URL: ${{ vars.SITE_URL || 'https://atsocy.com' }}

jobs:
  build-and-deploy:
    name: Build and deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        # `--ignore-scripts` blocks package install scripts (postinstall, etc.)
        # so a compromised dependency can't run arbitrary code in a job that
        # later exposes signing/deploy secrets. Astro/Vite don't need install
        # scripts to build.
        run: npm ci --ignore-scripts

      - name: Publish eligible posts to Nostr
        if: ${{ secrets.NOSTR_PRIVATE_KEY != '' }}
        env:
          NOSTR_PRIVATE_KEY: ${{ secrets.NOSTR_PRIVATE_KEY }}
          NOSTR_RELAYS: ${{ secrets.NOSTR_RELAYS }}
          CI: true
        run: npm run publish:nostr

      - name: Build site
        env:
          SITE_URL: ${{ vars.SITE_URL || 'https://atsocy.com' }}
          PUBLIC_USE_MOCK_EVENTS: 'false'
          PUBLIC_NOSTR_RELAYS: ${{ vars.PUBLIC_NOSTR_RELAYS || 'wss://relay.atsocy.com' }}
          PUBLIC_BLOSSOM_SERVERS: ${{ vars.PUBLIC_BLOSSOM_SERVERS || 'https://media.atsocy.com' }}
          PUBLIC_ATS_NPUB: ${{ vars.PUBLIC_ATS_NPUB || 'npub1hjn08rv4ymjgtpr6uzechwmzcfhet457vczxy8qlq2xaq836mutsp737yw' }}
          PUBLIC_HOT_PULSE_WINDOW_HOURS: ${{ vars.PUBLIC_HOT_PULSE_WINDOW_HOURS || '72' }}
        run: npm run build

      - name: Deploy to Cloudflare Pages
        if: ${{ secrets.CLOUDFLARE_API_TOKEN != '' && secrets.CLOUDFLARE_ACCOUNT_ID != '' }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler pages deploy dist --project-name atsocy-com
